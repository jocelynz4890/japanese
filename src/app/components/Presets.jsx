export default function Presets(){
    return (
        <div style={{ borderColor: 'var(--foreground)' }} className="flex flex-col border w-full h-1/4">
            buttons with settings/presets will go here
        {/* TODO: settings with global context */}
        {/* TODO: preset list + new preset */}
        </div>
    )
}