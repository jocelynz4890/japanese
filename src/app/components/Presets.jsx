export default function Presets(){
    return (
        <div className="flex flex-col border border-white w-full h-1/4">
            settings/presets will go here
        {/* TODO: settings with global context */}
        {/* TODO: preset list + new preset */}
        </div>
    )
}